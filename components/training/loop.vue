<template>
  <!-- <div>
    <div class="row">
      <div class="col-md-6 col-xs-12">
        <form @submit.prevent="addNewTodo">
          <label for="new-todo">Add Todo</label>
          <input type="text" v-model="new_todo" placeholder="E.g: I have to Eat" />
          <button type="submit" class="btn btn-success">Add Todo</button>
        </form>
      </div>
      <div class="col-md-6 col-xs-12">
        <ul>
          <li
            is="todoLoop"
            v-for="(todo, index) of todors"
            :key="todo.id"
            :title="todo.title"
            :remove="todos.splice(index, 1)"
          ></li>
        </ul>
      </div>
    </div>
  </div>-->
</template>
